[build]
  command = "PRISMA_SCHEMA=$(find ../.. -name schema.prisma | grep server | head -n 1) && npx prisma generate --schema=$PRISMA_SCHEMA && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
